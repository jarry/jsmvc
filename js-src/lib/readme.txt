lib 文件夹:

/*
 * @file core.js
 * @desc 底层静态函数类库，一般选用一个JS库，如jquery,prototype,tangram等
 *       <li>所有函数都是静态方法，UI组件可以是对象类</li>
 *       <li>把一些底层的方法含括进来，与业务和产品特性无关，可以适用任何项目</li>
 *       <li>与业务无关的代码，可以作为整个产品或项目共用</li>
 *
 */

此为示例而放上了好多的文件，实际操作中使用自己的类库即可。
本lib里面的js文件为以前的js文件，大部分文件存在问题。比如string.js,class,element.js，扩展了Object原型，同时对于元素初始化也并不友好，但是为了正常使用日历控件而采用这些文件。这些在本例当中是适用的，但是实际开发可以另外写lib类库。