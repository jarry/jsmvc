基本的通用函数库文件夹,这个文件的内容可以抽取放到所有模块外层，也可以单独放到模块内部，根据实际情况安排。

/*
 * @file com.js
 * @desc 产品中用到的静态公共函数库，有产品的特性
 *       <li>所有方法都是静态方法</li>
 *       <li>与具体业务逻辑无关，但有项目的特性，如验证规则、字符串处理方式、编码处理等</li>
 *       <li>避免重复以及处理规则不一致</li>
 *
 */


增加一个template.js文件